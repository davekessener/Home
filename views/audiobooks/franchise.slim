.container
	table
		tr
			td.thumb-column
				div.thumb-container
					img.thumbnail src="/resources/icons/audiobooks/#{franchise.path}.png"
			td.desc-column
				p.franchise =franchise.name
	.list-group
		- franchise.audiobooks.each do |book|
			a.btn.list-group-item.list-group-item-success href="/audiobooks/play/#{book.id}"
				.container-fluid
					.row
						.col-sm-8.col-xs-12
							span.pull-left
								=book.title
						.col-md-3.hidden-sm.hidden-xs
							span.pull-right
								="#{book.chapters.length} #{s('chapters')}"
						.col-md-1.col-sm-4.hidden-xs
							span.pull-right
								=Helper::to_s(book.duration)

